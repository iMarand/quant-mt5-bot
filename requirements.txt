pandas>=2.0
numpy>=1.24
PyYAML>=6.0
httpx>=0.24
scikit-learn>=1.3
lightgbm>=4.0

# Windows only — the MetaTrader 5 Python bridge. Everything except the MT5
# connector/broker works without it (use broker.kind=paper).
MetaTrader5>=5.0.45; sys_platform == "win32"

# Tests
pytest>=7.0
